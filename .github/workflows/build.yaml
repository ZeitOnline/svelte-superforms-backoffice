name: Build and Test
on:
  push:
    tags:
        - '*'

env:
  ENVIRONMENT: production
  PROJECT: spiele-backoffice
  IMAGE: backoffice
  REGISTRY: ${{ vars.GAR_DOCKER_REGISTRY }}

jobs:
  build:
    name: Build
    runs-on: zon-ubuntu-general-dind

    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: extraxt version
        shell: bash
        run: |
          REF=${{ github.ref_name }}
          TAG=${REF#${{ inputs.context }}-}
          echo "TAG=$TAG" >> $GITHUB_ENV

      - name: Baseproject
        id: baseproject
        uses: ZeitOnline/gh-action-baseproject@b86f75e115ac9546a653b58d3406c268ae3fb83b # v0.17.2
        with:
          project_name: ${{ env.PROJECT }}
          environment: ${{ env.ENVIRONMENT }}
          gar_docker_auth: true

      - name: Build and push image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: .
          file: Dockerfile
          push: true
          cache-from: type=gha
          cache-to: type=gha,mode=max
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE }}:${{ github.ref_name }}
            ${{ env.REGISTRY }}/${{ env.IMAGE }}:${{ github.sha }}
